[project]
name = "app-claudecode-tests"
version = "0.1.0"
description = "Integration test suite for the claude-code-remote daemon"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=43",
]

[tool.uv]
package = false
